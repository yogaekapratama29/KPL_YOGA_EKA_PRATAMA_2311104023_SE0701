#!/usr/bin/env node
"use strict";
var _build = require("./build");
var _init = require("./init");
var arg = process.argv[2];
if (arg === "build") {
    (0, _build).build();
} else {
    (0, _init).init();
}
